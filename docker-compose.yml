version: "3.8"

services:
  mysql:
    image: mysql:8
    environment:
      MYSQL_ROOT_PASSWORD: amit12345678
      MYSQL_DATABASE: musicdb
    ports:
      - "3307:3306"

  app:
    image: music-app
    ports:
      - "8081:8081"
    depends_on:
      - mysql